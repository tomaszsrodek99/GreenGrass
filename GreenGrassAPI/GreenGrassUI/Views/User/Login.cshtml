@model GreenGrassAPI.Dtos.UserLoginRequestDto
@{
    ViewData["Title"] = "Login";
}

<div class="text-center">
    <h2>Zaloguj</h2>
    <div class="blur-overlay" id="blurOverlay"></div>
    <form id="loginForm" asp-action="Login" asp-controller="User" class="my-form" data-loading="true">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="Email" class="control-label"></label>
            <div class="input-group">
                <span class="input-group-text"><i class="fa-solid fa-envelope"></i></span>
                <input type="email" asp-for="Email" class="form-control" />
            </div>
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Password" class="control-label"></label>
            <div class="input-group">
                <span class="input-group-text"><i class="fas fa-lock"></i></span>
                <input type="password" asp-for="Password" class="form-control" />
            </div>
            <span asp-validation-for="Password" class="text-danger"></span>
            <p class="form-text urlLink">
                <a href="/User/RegisterView">Nie masz konta? Zarejestruj się!</a>
            </p>
        </div>
        <div class="form-group">
            <div class="btn-group">
                <button type="submit" class="btn btn-primary" onclick="loading(2500)">Zaloguj</button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}